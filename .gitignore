node_modules/
node_modules
out/
dist/
.DS_Store
*.log
.vite/
.env
.env.local
.env.signing
.gstack/

# signing / notarization secrets — NEVER commit these
*.p8
*.p12
*.cer
*.mobileprovision
build/AuthKey_*.p8

# heavy hero source — only the compressed hero.mp4/.webm are deployed
docs/media/*.mov

# git worktree mounts for hive worker agents — never commit (each is a separate branch checkout)
.worktrees/

# raw demo screen recordings — only the compressed docs/media/demo/*.mp4 are deployed
docs/media/MunderDifflin*.mp4

# blog theme previews (local only)
docs/blog-preview-*/
# L0-FUSION mutant census scratch (test/automatic-submit.test.cjs); removed on exit, ignored in case of a crash
test/.mutants/
test/.mutants-delivery-hold/
test/.mutants-capacity-runtime/
test/.mutants-capacity-runtime-adm/
